# Copy to studio/.env. These point at the shared BKC Archive Sanity project and
# match the root .env.local — leave them as-is.
#
# Only SANITY_STUDIO_*-prefixed vars are exposed to the Studio bundle.

SANITY_STUDIO_PROJECT_ID=lwvigy05
SANITY_STUDIO_DATASET=production
